# Branch snapshots

This directory holds flat-file snapshots of old feature branches, taken
before this repo's git history was squashed. Nobody has gone through them
since the squash. Each file under `branches/` is one branch's last known
state and a short human-written note about its status.
